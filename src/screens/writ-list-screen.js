import React, { useState } from 'react';
import {View, TextInput, Button, StyleSheet, TouchableOpacity, Text} from 'react-native';


const WritListScreen = ({ navigation: { navigate } }) => {
 
    return (
    <>
        <Text>This is where the complete list of writs that the server should have in their possession.</Text>
    </>
    )}


export default WritListScreen;